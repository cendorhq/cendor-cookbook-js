{
  "name": "cendor-recipe-spotlight-untrusted-docs",
  "private": true,
  "type": "module",
  "description": "Cendor cookbook - wrap retrieved content so the model treats it as data, not orders (JS, offline).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node index.mjs"
  },
  "dependencies": {
    "@cendor/core": "^3.4.0",
    "@cendor/guardrails": "^3.1.0"
  }
}
