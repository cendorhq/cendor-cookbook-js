{
  "name": "cendor-recipe-tokenguard",
  "private": true,
  "type": "module",
  "description": "Cendor cookbook - stop a runaway agent loop before it overspends (JS, offline).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node index.mjs"
  },
  "dependencies": {
    "@cendor/core": "^3.4.0",
    "@cendor/tokenguard": "^3.1.0"
  }
}
