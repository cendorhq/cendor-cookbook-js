{
  "name": "cendor-recipe-batch-ingest",
  "private": true,
  "type": "module",
  "description": "Cendor cookbook - ship spend records to your own store in batches (JS, offline).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node index.mjs"
  },
  "dependencies": {
    "@cendor/core": "^3.4.0",
    "@cendor/tokenguard": "^3.1.0"
  }
}
