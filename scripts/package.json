{
  "name": "cendor-cookbook-js-tooling",
  "private": true,
  "type": "module",
  "description": "Maintainer tooling for the cookbook. NOT a workspace, NOT a recipe dependency — no recipe ever resolves anything from here. Versions are pinned EXACTLY (no carets) on purpose: check-ts-js-sync compares generated bytes, so a floating generator version would turn a green gate red on somebody else's machine.",
  "scripts": {
    "build": "node build-recipes.mjs",
    "check": "node check-ts-js-sync.mjs"
  },
  "dependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/node": "22.10.2",
    "ts-blank-space": "0.9.0",
    "typescript": "5.7.3"
  }
}
